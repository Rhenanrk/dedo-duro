<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufg.com.dedoduro">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dedo_duro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dedo_duro_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".model.HomeActivity" />
        <activity android:name=".model.NewRegisterActivity" />
        <activity android:name=".model.DescricaoObraActivity" />
        <activity android:name=".model.EdicaoObraActivity"></activity>
    </application>

</manifest>